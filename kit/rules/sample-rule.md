# Sample Rule

Project convention seeded by vcskill. Providers that consume `AGENTS.md` get
this merged into a managed block; Claude keeps it under `.claude/rules/`.

- Prefer small, focused modules.
- Never commit secrets.
