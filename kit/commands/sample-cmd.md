---
description: Sample slash command that runs the echo skill against its arguments.
argument-hint: "[text]"
agent: sample-reviewer
---

# /sample-cmd

Run the echo flow on `$ARGUMENTS`. If no arguments are given, ask via
`AskUserQuestion`. Skill assets live under `.claude/skills/`.
